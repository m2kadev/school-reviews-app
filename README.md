# Google Review Scraper

A simple Python script that fetches school reviews using the Google Maps API.

## Run Locally
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
